@page "/"
@code {
    [Inject] public NavigationManager _manager { get; set; } = null;

    protected  override void OnInitialized()
    {
        _manager.NavigateTo("/Login", forceLoad:true);
    }
}